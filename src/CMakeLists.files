set (QT_HEADER_FILES
	forms/select_profile.h
)

set (QT_FORMS
	forms/select_profile.ui
)

set (QT_RESOURCES
)

set (HEADER_FILES
	${QT_HEADER_FILES}
	core/auth_scope.h
	core/class_bitset.h
	core/first_time.h
	forms/form_base.h
	forms/form_base.hpp
	config.h
)

set (SOURCE_FILES
	core/auth_scope.cpp
	forms/select_profile.cpp
	main.cpp
)

source_group("Source Files" FILES ${SOURCE_FILES})
source_group("Header Files" FILES ${HEADER_FILES})
source_group("Forms" FILES ${QT_FORMS})
source_group("Resources" FILES ${QT_RESOURCES})
source_group("" FILES CMakeLists.txt CMakeLists.files)
